# Movie OTP Frontend (React)

## Quick start
1. Install Node.js and npm.
2. In `frontend` folder run:
   ```
   npm install
   npm start
   ```
3. The app runs at http://localhost:3000 and calls backend at http://localhost:8080

Note: For demo, backend prints OTP to console and also returns it in response. In production remove OTP from responses.
